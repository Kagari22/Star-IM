# 星讯（Star-IM）即时通讯系统 — 功能与技术总结

## 一、项目概述

星讯（Star-IM）是一个从零实现的分布式即时通讯系统，提供单聊、群聊、AI 聊天好友、抢红包、消息全文检索等完整 IM 能力。

- **技术栈**：Go / Gin / GORM / MySQL / Redis / RabbitMQ / MinIO / Elasticsearch / Vue 3 / Vite / Docker Compose
- **架构风格**：前后端分离，后端采用经典分层架构（Handler → Service → Repository），单体部署、多节点可扩展
- **代码规模**：后端约 8500 行 Go 代码，前端为 Vue 3 单页应用

---

## 二、整体架构

```
┌─────────────────────────────────────────────┐
│                 前端 (Vue 3 SPA)             │
│  登录/注册 · 好友列表 · 聊天窗口 · 红包 · 搜索 │
└──────────┬──────────────────┬───────────────┘
           │ REST API (JSON)  │ WebSocket 长连接
┌──────────▼──────────────────▼───────────────┐
│              后端 (Go + Gin)                 │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌───────┐ │
│  │Handler │→│Service │→│Repository│→│ MySQL │ │
│  │ 鉴权/参数│ │ 业务逻辑│ │ 数据访问 │ │持久化 │ │
│  └────────┘ └────────┘ └────────┘ └───────┘ │
│   AI(DeepSeek) · 搜索(ES) · 存储(MinIO)      │
│   消息队列(RabbitMQ) · 缓存/状态(Redis)      │
└─────────────────────────────────────────────┘
```

**分层职责**：Handler 负责鉴权与参数校验；Service 承载业务规则与事务边界；Repository 通过接口抽象数据访问，MySQL 为唯一实现，便于替换与测试。

---

## 三、核心功能模块与业务流程

### 3.1 用户认证与账户管理

- **功能**：注册、登录、登出、个人资料（昵称/头像/密码）修改、JWT 身份认证。
- **数据输入输出**：`POST /api/register`、`POST /api/login` 接收用户名/密码/昵称，返回 JWT Token 与用户信息；`GET /api/me` 返回当前用户。
- **关键实现**：密码 bcrypt 哈希存储；登录签发 JWT；登出将 Token 加入 Redis 黑名单实现即时失效；登录/注册/改密等接口按 IP 限流。

### 3.2 好友与社交关系

- **功能**：好友申请/同意/拒绝、好友列表、删除好友、搜索用户、搜索并申请加入群聊。
- **数据输入输出**：`POST /api/friend-requests` 发起申请，`GET /api/friend-requests` 查看待处理，`POST /api/friend-requests/:id/respond` 响应；`GET /api/users` 返回好友列表。
- **交互逻辑**：申请 → 对方待处理列表可见 → 同意后双向建立 `friendships` 记录，前端刷新好友列表。

### 3.3 单聊消息

- **功能**：实时收发、历史消息分页加载、离线消息补偿、已读回执、撤回（限时）、编辑、收藏、引用回复、删除会话消息。
- **数据输入输出**：上行经 WebSocket 发送 `{type:"chat", to, content}`，服务端回 `ack`；下行由服务端推送 `{type:"chat", message}`；历史通过 `GET /api/messages` 分页拉取。
- **交互逻辑**：发送 → 服务端校验接收方存在 → 事务写入 `messages` 与 `outbox_events` → 更新 Redis 未读 → RabbitMQ 发布事件 → 消费端按在线状态实时推送；发送者经 ack 确认；离线用户通过 `GET /api/offline` 补偿拉取。
- **状态管理**：未读数存 Redis（`unread/redis`），已读位置存 `conversation_reads` 表；前端本地维护消息数组并按 ID 排序去重。

### 3.4 群聊

- **功能**：创建群（含拉人）、成员列表、邀请/移除成员、退出、解散、群公告、全员禁言、成员角色（群主/管理员/普通）、按成员禁言、群消息实时推送。
- **数据输入输出**：`POST /api/groups` 建群；`GET /api/groups/:id/messages` 拉群消息；群消息经 WebSocket 的 `group_id` 字段发送。
- **关键实现**：建群在事务内同时写 `chat_groups` 与 `group_members`；发送群消息时校验成员身份与禁言状态；推送时遍历在线成员分发（`DispatchMessageCreated`），离线者下次拉取补偿。

### 3.5 AI 聊天好友

- **功能**：内置 AI 机器人账号（`ai_assistant`），自动补齐为所有用户好友；基于 DeepSeek 大模型对话，支持联网搜索、实时天气、日期查询。
- **数据输入输出**：用户给 AI 好友发消息 → 服务端携带最近 N 条上下文调用大模型 → 模型工具调用（function calling）执行 `web_search` / `get_weather` / `get_today` → 结果回传模型 → 最终回复经消息链路返回用户。
- **关键实现**：OpenAI 兼容接口封装（`ai` 包）；工具执行（`tools.go`）中天气走 Open-Meteo（带重试、中国城市优先），搜索走 Bing（按当天日期过滤结果）；对话超时与工具轮数上限控制，防止死循环。

### 3.6 抢红包

- **功能**：余额钱包（仅支持兑换码充值）、发拼手气红包/普通红包、抢红包、红包领取详情。
- **数据输入输出**：`POST /api/redeem` 兑换码充值；`POST /api/groups/:id/red-packets` 发红包；`POST /api/red-packets/:id/grab` 抢红包；`GET /api/red-packets/:id` 查详情；`GET /api/balance` 查余额。
- **交互逻辑**：兑换码 `114514` 兑换 100 元 → 发红包（事务内扣减余额并创建红包）→ 群成员点击抢（行锁扣减剩余 + 记领取记录 + 余额入账）→ 红包作为群消息实时推送，前端展示祝福语与"已领取"状态。
- **状态管理**：金额一律以整数「分」存储，前端展示时换算为元；前端缓存红包详情用于判断是否已领。

### 3.7 消息全文检索

- **功能**：对单聊/群聊消息做关键词全文搜索，支持按会话范围过滤。
- **数据输入输出**：`GET /api/search/messages?q=关键词&peer_id=/group_id=`。
- **关键实现**：消息落库后经 RabbitMQ 事件异步写入 Elasticsearch；搜索时查 ES 返回消息 ID 列表，再回查 MySQL 组装完整消息。索引器可关闭（NoopIndexer），不影响主流程。

### 3.8 媒体文件

- **功能**：图片/文件上传与下载、头像上传。
- **数据输入输出**：`POST /api/media/upload`（multipart）→ MinIO 对象存储，返回预签名下载 URL（15 分钟有效）；`POST /api/me/avatar` 上传头像。
- **异常处理**：文件大小上限、类型白名单校验（仅图片类）、上传进度反馈；未启用 MinIO 时降级为 NoopUploader。

### 3.9 在线状态与未读

- **功能**：好友在线/离线展示、会话未读数。
- **关键实现**：WebSocket 连接建立时向 Redis 写入"用户 → 节点"映射（`presence/redis`），前端轮询 `GET /api/presence` 获取状态；未读数由消息推送时对非当前会话累加。

---

## 四、关键技术与实现要点

### 4.1 可靠消息投递（Outbox + RabbitMQ）

- 消息保存与事件发布在同一数据库事务内完成：`messages` 表写入后同时写入 `outbox_events`，杜绝"库已存、事件没发"的丢失场景。
- 独立的 Outbox Dispatcher 轮询未发布事件并推入 RabbitMQ，收到 broker 确认后标记 `published_at`；消费者按事件驱动 WebSocket 推送与 ES 索引，实现"发送方 ack / 接收方推送 / 离线补偿"三路闭环。

### 4.2 高并发抢红包

- **随机金额**：拼手气红包采用「二倍均值法」——每次随机范围上限 = min(剩余均值 × 2, 剩余金额 − (剩余人数 − 1))，保证每人至少 1 分且总额分毫不差；最后一个领取者获得全部剩余。
- **并发安全**：领取操作在数据库事务内对红包行加 `SELECT ... FOR UPDATE` 排他锁，串行化同一红包的并发抢；`(packet_id, user_id)` 唯一索引兜底防重复领取；扣余额/扣剩余用条件更新（`WHERE balance >= amount`）防止余额为负。
- **金额精度**：全链路以整数「分」计算，规避浮点误差。

### 4.3 大模型工具调用（function calling）

- 系统提示词明确 AI 的能力边界与回答纪律（必须调用工具获取真实数据、禁止编造、回复纯文本）。
- 工具执行异常（网络失败、城市找不到）会回传模型，由模型如实说明，而非硬编码兜底；搜索限定当天结果并附来源。

### 4.4 鉴权与限流

- JWT 无状态认证；登出黑名单（Redis）实现 Token 即时失效；WebSocket 握手校验 Token 并复用黑名单。
- 分层限流：注册/登录/消息发送/搜索等接口按 IP 限流（Redis 计数器），消息发送按用户限流（每分钟上限），防止滥用。

### 4.5 状态管理

- **前端**：Vue 3 Composition API（`ref`/`computed`），消息、会话、在线状态均为响应式；消息按 ID 去重排序；主题（亮/暗）通过 CSS 变量切换，并同步到 `<html data-theme>` 以覆盖 Teleport 弹窗。
- **后端**：JWT（身份，无状态）＋ Redis（在线状态、黑名单、限流、未读、多节点定位）＋ MySQL（消息、关系、红包等持久化），各司其职。

---

## 五、性能优化

- **数据库**：连接池复用；历史消息分页（`after_id` + `limit`）避免全量加载；群成员/消息/收藏等常用查询建索引。
- **缓存降载**：在线状态与未读数放 Redis，避免高频状态轮询压垮 MySQL。
- **消息推送**：仅推送给在线且位于当前节点的用户（Redis 节点映射），离线者走补偿拉取，避免无效推送。
- **抢红包**：行锁只在红包行上，不同红包可并行，吞吐可控。
- **前端**：消息增量追加 + 虚拟滚动式裁剪（仅加载最近 100 条）；图片懒加载、媒体预签名 URL 直连下载。

---

## 六、异常处理与容错

- **统一错误响应**：`httpx.WriteError` 规范 JSON 错误体，Handler 层区分 4xx（参数/权限/业务）与 5xx（系统）。
- **幂等与并发**：`INSERT IGNORE`（收藏/置顶）、唯一约束、条件更新（余额/剩余）保证并发下数据一致；兑换码"原子标记 + 判断影响行数"防重复兑换。
- **网络容错**：AI 工具调用带超时与重试（Open-Meteo 不稳定时最多重试 3 次）；WebSocket 断线自动重连（1.8s 退避）；媒体下载失败不阻塞主流程。
- **降级策略**：未启用 MinIO / Elasticsearch / RabbitMQ / AI 时分别降级为 NoopUploader、NoopIndexer、NoopPublisher 与关闭聊天，保证基础功能可用。
- **数据一致性**：抢红包、发红包、兑换码均在事务内完成；红包消息推送与红包创建拆分为两步，推送失败不阻断发红包。

---

## 七、模块关系与数据流

```
用户/前端
   │ WebSocket / REST
   ▼
认证(黑名单) → 社交/群 → 消息(保存+Outbox) → RabbitMQ → 消费 → 推送/索引
   │                        │
   │                        ├─→ Redis 未读/在线状态
   │                        ├─→ MySQL messages/outbox_events
   │                        └─→ ES 索引（搜索）
   ▼
红包(余额/兑换码) → MySQL red_packets/receipts → 红包消息复用消息链路
AI 好友 → DeepSeek(function calling) → web_search / get_weather / get_today
```

**关键依赖**：消息模块是核心枢纽，同时被单聊、群聊、红包消息、AI 回复复用；社交与群模块为消息提供成员/好友校验；可靠投递层（Outbox + RabbitMQ）解耦了"业务落库"与"异步推送/索引"，是整体一致性的基石。

---

*数据表：users、chat_groups、group_members、messages、outbox_events、friendships、friend_requests、group_join_requests、message_favorites、conversation_reads、conversation_preferences、group_kick_logs、redeem_codes、red_packets、red_packet_receipts 等。*
